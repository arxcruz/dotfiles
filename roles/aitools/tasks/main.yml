---
# npm's default global prefix (/usr/lib/node_modules on this Nodesource
# install) requires root. Point it at a user-writable directory instead of
# relying on the desktop-only system/npm-global.yml task, since this role
# also runs on servers where that task never gets included.
- name: Ensure ~/.npm-global directory exists
  file:
    path: "{{ ansible_facts.user_dir }}/.npm-global"
    state: directory
    mode: '0755'

- name: Set npm global prefix to user-local directory
  command: "npm config set prefix {{ ansible_facts.user_dir }}/.npm-global"
  changed_when: false

- name: Install/update Claude Code CLI
  npm:
    name: "@anthropic-ai/claude-code"
    global: true
    state: latest

- name: Ensure .claude directory exists
  file:
    path: "{{ ansible_facts.user_dir }}/.claude"
    state: directory
    mode: '0755'

- name: Deploy Claude Code statusline.sh
  copy:
    src: claude-statusline.sh
    dest: "{{ ansible_facts.user_dir }}/.claude/statusline.sh"
    mode: '0755'

- name: Check if Claude Code settings.json exists
  stat:
    path: "{{ ansible_facts.user_dir }}/.claude/settings.json"
  register: claude_settings_stat

- name: Read Claude Code settings.json if present
  slurp:
    path: "{{ ansible_facts.user_dir }}/.claude/settings.json"
  register: claude_settings_file
  when: claude_settings_stat.stat.exists

- name: Ensure statusLine is configured in Claude Code settings.json
  copy:
    content: >-
      {{
        ((claude_settings_file.content | default('') | b64decode | from_json)
         if (claude_settings_stat.stat.exists and (claude_settings_file.content | default('') | b64decode | trim != ''))
         else {})
        | combine({
            'statusLine': {
              'type': 'command',
              'command': '~/.claude/statusline.sh'
            }
          }, recursive=True)
        | to_nice_json(indent=2)
      }}
    dest: "{{ ansible_facts.user_dir }}/.claude/settings.json"
    mode: '0644'

- name: Install/update antigravity-cli
  shell: curl -fsSL https://antigravity.google/cli/install.sh | bash
  changed_when: false

- name: Ensure antigravity-cli directory exists
  file:
    path: "{{ ansible_facts.user_dir }}/.gemini/antigravity-cli"
    state: directory
    mode: '0755'

- name: Deploy antigravity-cli statusline.sh
  copy:
    src: antigravity-statusline.sh
    dest: "{{ ansible_facts.user_dir }}/.gemini/antigravity-cli/statusline.sh"
    mode: '0755'

- name: Check if antigravity-cli settings.json exists
  stat:
    path: "{{ ansible_facts.user_dir }}/.gemini/antigravity-cli/settings.json"
  register: antigravity_settings_stat

- name: Read antigravity-cli settings.json if present
  slurp:
    path: "{{ ansible_facts.user_dir }}/.gemini/antigravity-cli/settings.json"
  register: antigravity_settings_file
  when: antigravity_settings_stat.stat.exists

- name: Ensure statusLine is configured in antigravity-cli settings.json
  copy:
    content: >-
      {{
        ((antigravity_settings_file.content | default('') | b64decode | from_json)
         if (antigravity_settings_stat.stat.exists and (antigravity_settings_file.content | default('') | b64decode | trim != ''))
         else {})
        | combine({
            'statusLine': {
              'type': 'command',
              'command': '~/.gemini/antigravity-cli/statusline.sh'
            }
          }, recursive=True)
        | to_nice_json(indent=2)
      }}
    dest: "{{ ansible_facts.user_dir }}/.gemini/antigravity-cli/settings.json"
    mode: '0644'

- name: Enable linger so user systemd services start on boot without login
  command: loginctl enable-linger {{ ansible_facts.user_id }}
  changed_when: false

- name: Ensure podman quadlet directory exists
  file:
    path: "{{ ansible_facts.user_dir }}/.config/containers/systemd"
    state: directory
    mode: '0755'

- name: Deploy ai-memory podman quadlet unit
  copy:
    src: ai-memory.container
    dest: "{{ ansible_facts.user_dir }}/.config/containers/systemd/ai-memory.container"
    mode: '0644'
  register: ai_memory_quadlet

- name: Reload systemd user daemon if ai-memory quadlet changed
  systemd:
    daemon_reload: true
    scope: user
  when: ai_memory_quadlet.changed

- name: Ensure ai-memory service is enabled and running
  systemd:
    name: ai-memory.service
    scope: user
    enabled: true
    state: started
