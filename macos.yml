---
- name: Starting macOS configuration
  hosts: all
  vars_files:
  - "{{ lookup('first_found', [ 'vars/{{ ansible_distribution|lower }}.yml', 'vars/default.yml']) }}"
  vars:
    is_server: false
    build_neovim: false


  roles:
    - { role: 'system', tags: ['system'] }
    - { role: 'neovim', tags: ['neovim'] }
    - { role: 'tmux', tags: ['tmux'] }
    - { role: 'zsh', tags: ['zsh'] }
